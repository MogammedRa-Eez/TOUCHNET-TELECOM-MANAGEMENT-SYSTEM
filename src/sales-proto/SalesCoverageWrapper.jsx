/**
 * Thin wrapper — renders the existing CoverageCheck page inside the Sales prototype.
 * No logic changes; just re-exports the page so the sales routes can use it.
 */
export { default } from "@/pages/CoverageCheck";